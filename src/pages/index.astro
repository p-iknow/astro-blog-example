---
import IndexPage from "@/pages.src/index-page.astro";
import { getAllBlogs } from "@/content/content-collections";
import { EnLocale } from "@/config/locale";

const posts = await getAllBlogs({ lang: EnLocale });
---

<IndexPage posts={posts} lang={EnLocale} />;
